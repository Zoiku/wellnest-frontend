const styling = (style) => {
  const styles = {
    styles: {
      textTransform: "capitalize",
      fontWeight: 600,
    },
    variant: "outlined",
  };

  return styles;
};

export default styling;
